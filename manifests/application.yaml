apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: jump-ui
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/ajay-devops-cloud/github-k8s-deployment
    path: manifests
    targetRevision: main
  destination:
    server: https://kubernetes.default.svc
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      enabled: true